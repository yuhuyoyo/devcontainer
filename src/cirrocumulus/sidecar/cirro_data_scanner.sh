#!/bin/bash

# cirro_data_scanner scans the shared volumes for data folders with cirro supported file type
# and create dataset.
scan_for_cirro_data() {
  echo "hello"
}

while true; do
  scan_files
  sleep 5
done
